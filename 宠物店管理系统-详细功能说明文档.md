# 宠物店管理系统 - 详细功能说明文档

> 本文档在原始需求基础上对功能进行详细完善，供 Cursor / AI 辅助开发使用。开发时请严格按本文档实现后端接口、前端页面及数据库设计。

---

## 一、项目概述

本系统采用 **SpringBoot + Vue** 前后端分离架构，为宠物店提供：系统登录、首页数据可视化、会员管理、会员充值、宠物类型与档案、疫苗与医疗记录、商品管理、订单与索赔、管理员管理等一体化能力。**仅支持管理员账号登录**，所有功能基于登录后的权限体系展开。

---

## 二、技术栈与运行环境

| 分类 | 技术/版本 |
|------|-----------|
| 后端框架 | SpringBoot + MyBatis-Plus |
| 认证 | JWT |
| 序列化 | Fastjson |
| 前端框架 | Vue + Element-UI |
| 请求/图表 | Axios + ECharts |
| 开发工具 | IntelliJ IDEA（后端）、VS Code（前端） |
| 运行环境 | JDK 1.8、Maven 3.6.3、MySQL 8.0、Node.js 14.21.3 |

---

## 三、后端工程目录结构

```
springboot-base/
├── src/main/java/com/jsonll/base/
│   ├── controller/     # 各模块 Controller
│   ├── core/           # 统一返回 R、全局异常等
│   ├── entity/         # 实体类，与表一一对应
│   ├── mapper/         # MyBatis-Plus Mapper 接口
│   ├── request/        # 分页/筛选请求参数
│   ├── service/        # 接口 + impl 实现
│   ├── utils/          # 工具类
│   └── JsonBaseApplication.java
├── src/main/resources/
│   ├── application.properties / application.yml
│   └── mapper/         # 可选，复杂 SQL 的 xml
└── pom.xml
```

---

## 四、后端接口开发规范（必遵）

- 控制器：`@RestController` + `@RequestMapping("/模块路径")`，如 `/member`、`/recharge`。
- 统一返回：所有接口返回自定义 **R** 对象（如 `code`、`msg`、`data`）。
- 接口约定：
  - 分页列表：`GET /模块/pageList`，入参为各模块的 Request 对象（含 `current`、`size` 及筛选字段），返回分页结果。
  - 新增：`POST /模块/add`，`@RequestBody` 实体。
  - 更新：`PUT /模块/update`，`@RequestBody` 实体。
  - 删除：`DELETE /模块/delete`，`@RequestParam("id")` 主键。
  - 详情：`GET /模块/detail`，`@RequestParam("id")` 主键，返回单条数据。
- 依赖注入：Controller 中 `@Autowired` 对应 Service 接口。

---

## 五、核心功能模块详细说明

### 5.1 系统登录模块

| 项目 | 说明 |
|------|------|
| 访问地址 | `localhost:8080`（或前端配置的端口），唯一登录入口 |
| 默认账号 | 账号：`admin`，密码：`admin` |
| 前端要素 | 账号输入框、密码输入框、登录按钮 |
| 后端接口 | `POST /login` 或 `POST /admin/login`：入参 `username`、`password`，校验通过返回 JWT 或 token，以及用户基本信息（如姓名、角色） |
| 校验逻辑 | 校验账号存在、密码正确；失败返回明确提示（账号不存在/密码错误） |
| 成功行为 | 前端存储 token，跳转至首页；请求头携带 token 访问后续接口 |

**建议实体/表**：管理员表 `sys_admin`（或与管理员管理共用），字段含：id、账号、密码（加密存储）、姓名、角色、状态、创建时间等。

---

### 5.2 首页模块

| 项目 | 说明 |
|------|------|
| 布局 | 左侧菜单导航 + 顶部栏（当前登录角色）+ 主内容区 |
| 主内容 | 欢迎语 + 统计卡片 + 快捷入口 |
| 统计卡片 | 宠物档案总数、订单总数、总收入、会员总数（数字卡片，可后端聚合接口 `GET /home/statistics` 返回） |
| 快捷入口 | 会员管理、宠物档案、订单管理、商品管理等跳转链接/按钮 |
| 菜单项 | **会员中心**：会员管理、会员生命周期、充值流水、卡类型管理、会员卡、卡交易流水；**宠物服务**：宠物类型、宠物档案、疫苗记录、医疗记录；**商品与采购**：商品管理、供应商管理、采购管理；**库存管理**：库存管理、库存预警、库存盘点、库存流水；**销售与财务**：订单管理、索赔记录、财务报表；**系统设置**：数据字典、管理员管理。点击跳转对应路由。 |

---

### 5.3 会员管理模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/member`；前端路由：如 `/member/list` |
| 功能 | 会员的查询、新增、编辑、删除、详情 |

**列表与查询**

- 搜索：支持按 **会员姓名、手机号、会员编号** 关键词（可单字段或组合，后端用 LIKE）。
- 列表字段：序号、会员编号、姓名、手机号、会员等级、注册时间、状态等；操作列：详情、编辑、删除。
- 分页：默认每页 10 条，支持页码、上一页/下一页、每页条数选择。

**新增/编辑表单字段（建议）**

- 会员编号（可自动生成或可填）、姓名（必填）、手机号（必填、格式校验）、会员等级（下拉/字典）、性别、生日、地址、备注、状态等。

**后端接口**

- `GET /member/pageList`：Request 含 `current`、`size`、`name`、`phone`、`memberNo` 等。
- `POST /member/add`、`PUT /member/update`：RequestBody 为会员实体。
- `DELETE /member/delete?id=xxx`、`GET /member/detail?id=xxx`。

**数据库表建议**：`member`（id、member_no、name、phone、level、gender、birthday、address、status、create_time、update_time 等）。

---

### 5.4 会员充值模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/recharge`；前端：如 `/recharge/list` |
| 功能 | 充值记录的查询、新增、详情查看（可不做编辑/删除，以财务记录为准） |

**列表与查询**

- 搜索：订单号、会员姓名、手机号（关键词）；时间范围：开始时间 ~ 结束时间（创建时间或充值时间）。
- 按钮：搜索、重置。
- 列表字段：序号、订单号、会员姓名、手机号、充值金额、实付金额、备注、创建时间；操作列：详情。
- 分页：默认 10 条/页，支持页码与每页条数。

**新增充值**

- 按钮：“新增充值”。弹窗表单：选择/输入会员（姓名或手机号可联想）、充值金额、实付金额、备注（如充送活动）。提交后生成充值记录，订单号可后端生成（如时间戳+随机数）。

**后端接口**

- `GET /recharge/pageList`：Request 含 `current`、`size`、`orderNo`、`memberName`、`phone`、`startTime`、`endTime`。
- `POST /recharge/add`：RequestBody 含会员标识、充值金额、实付金额、备注等。
- `GET /recharge/detail?id=xxx`。

**数据库表建议**：`recharge_record`（id、order_no、member_id、member_name、phone、amount、pay_amount、remark、create_time 等）。

---

### 5.5 宠物类型模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/petType`；前端：如 `/petType/list` |
| 功能 | 宠物分类（如猫、狗）的查询、新增、编辑、删除、详情 |

**列表与查询**

- 搜索：宠物类型名称（关键词）。
- 列表字段：序号、类型 ID、类型名称、创建时间、状态；操作：详情、编辑、删除。
- 分页：同上。

**表单字段**

- 类型名称（必填）、排序、状态（启用/禁用）等。

**后端接口**

- `GET /petType/pageList`：Request 含 `current`、`size`、`typeName`。
- `POST /petType/add`、`PUT /petType/update`、`DELETE /petType/delete?id=xxx`、`GET /petType/detail?id=xxx`。

**数据库表建议**：`pet_type`（id、type_name、sort、status、create_time、update_time）。

---

### 5.6 宠物档案模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/pet`；前端：如 `/pet/list` |
| 功能 | 宠物档案的查询、新增、编辑、删除、详情 |

**列表与查询**

- 搜索：宠物名称、联系人、联系电话（关键词）；搜索、重置。
- 列表字段：序号、宠物名称、宠物类型、宠物品种、毛色、联系人、联系电话；操作：详情、编辑、删除。
- 分页：默认 10 条/页。

**新增/编辑表单**

- 宠物名称、宠物类型（关联宠物类型表下拉）、宠物品种、毛色、性别、出生日期、联系人、联系电话（必填、格式校验）、会员（可选关联）、备注等。

**后端接口**

- `GET /pet/pageList`：Request 含 `current`、`size`、`petName`、`contactName`、`contactPhone`。
- `POST /pet/add`、`PUT /pet/update`、`DELETE /pet/delete?id=xxx`、`GET /pet/detail?id=xxx`。

**数据库表建议**：`pet`（id、pet_name、type_id、breed、color、gender、birthday、contact_name、contact_phone、member_id、remark、create_time、update_time）。

---

### 5.7 疫苗记录模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/vaccine`；前端：如 `/vaccine/list` |
| 功能 | 疫苗接种记录的查询、新增、编辑、删除、详情 |

**列表与查询**

- 搜索：疫苗名称、宠物名称、接种医生（关键词）；时间范围：开始日期 ~ 结束日期（接种日期）；搜索、重置。
- 列表字段：序号、宠物名称、疫苗名称、疫苗批号、接种日期、接种剂次、接种部位、接种医生、费用金额；操作：详情、编辑、删除。
- 分页：默认 10 条/页。

**新增/编辑表单**

- 宠物（下拉选择，可关联宠物档案）、疫苗名称、疫苗批号、接种日期、接种剂次、接种部位、接种医生、费用金额（数字校验）等。

**后端接口**

- `GET /vaccine/pageList`：Request 含 `current`、`size`、`vaccineName`、`petName`、`doctor`、`startDate`、`endDate`。
- `POST /vaccine/add`、`PUT /vaccine/update`、`DELETE /vaccine/delete?id=xxx`、`GET /vaccine/detail?id=xxx`。

**数据库表建议**：`vaccine_record`（id、pet_id、pet_name、vaccine_name、batch_no、vaccine_date、dose、part、doctor、fee、create_time、update_time）。

---

### 5.8 医疗记录模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/medical`；前端：如 `/medical/list` |
| 功能 | 宠物医疗诊治记录的查询、新增、编辑、删除、详情 |

**列表与查询**

- 搜索：宠物名称、主治医生、主要症状（关键词）；时间范围：开始日期 ~ 结束日期（就诊日期）；搜索、重置。
- 列表字段：序号、宠物名称、就诊日期、主要症状/主诉、初步诊断、最终诊断、主治医生；操作：详情、编辑、删除。
- 分页：默认 10 条/页。

**新增/编辑表单**

- 宠物（下拉）、就诊日期、主要症状/主诉、初步诊断、最终诊断、主治医生、处方/处置说明、费用（可选）等。

**后端接口**

- `GET /medical/pageList`：Request 含 `current`、`size`、`petName`、`doctor`、`symptom`、`startDate`、`endDate`。
- `POST /medical/add`、`PUT /medical/update`、`DELETE /medical/delete?id=xxx`、`GET /medical/detail?id=xxx`。

**数据库表建议**：`medical_record`（id、pet_id、pet_name、visit_date、symptom、diagnosis_initial、diagnosis_final、doctor、prescription、fee、create_time、update_time）。

---

### 5.9 商品管理模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/goods`；前端：如 `/goods/list` |
| 功能 | 记录所有可零售的**商品和服务**主数据：查询、新增、编辑、删除、详情。**不在此记录库存量**，库存由「库存管理」维护。 |

**列表与查询**

- 搜索：商品名称、商品编码、品牌、分类（关键词）；搜索、重置。
- 列表字段：序号、商品名称、商品编码、分类、品牌、售价、原价、商品简介；操作：详情、编辑、删除。（不展示库存）
- 分页：默认 10 条/页。

**新增/编辑表单**

- 商品名称、商品编码、分类（字典）、品牌、售价、原价、商品简介、单位、状态等；金额需前端校验。**不包含库存字段**。

**后端接口**

- `GET /goods/pageList`：Request 含 `current`、`size`、`goodsName`、`goodsCode`、`brand`、`category`。
- `POST /goods/add`、`PUT /goods/update`、`DELETE /goods/delete?id=xxx`、`GET /goods/detail?id=xxx`。

**数据库表建议**：`goods`（id、goods_name、goods_code、brand、price、original_price、stock、intro、unit、category、status、create_time、update_time）。其中 `stock` 由库存逻辑维护：采购入库增加、订单出库减少，商品管理界面不展示、不编辑。

---

### 5.10 订单管理模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/order`；前端：如 `/order/list` |
| 功能 | 订单的**查询、新增、编辑、删除**、详情、订单状态更新；**必须关联商品管理中的商品**（订单明细选择商品、数量、单价）。订单状态改为「已发货」或「已完成」时**出库扣减库存**。 |

**列表与查询**

- 筛选：订单号、宠物名称、会员姓名、订单时间范围、订单状态（下拉）；搜索、重置。
- 列表字段：序号、订单号、会员姓名、宠物名称（若有）、商品信息（可摘要）、订单金额、下单时间、订单状态；操作：详情、编辑、删除、状态修改。
- 分页：默认 10 条/页。

**新增/编辑订单**

- 选择会员（可选）、宠物（可选）；**订单明细**：从商品管理中选择商品、填写数量、自动带出单价并计算小计；支持多行明细，总金额自动汇总。
- 提交后写入 `order_main` 与 `order_item`。**库存扣减**：在订单状态更新为「已发货」或「已完成」时统一出库扣减库存并写库存流水。

**详情与状态**

- 详情：展示订单头 + 订单明细（商品、数量、单价、小计）。
- 状态修改：弹窗选择目标状态并提交。

**后端接口**

- `GET /order/pageList`、`GET /order/detail?id=xxx`。
- `POST /order/add`、`PUT /order/update`、`DELETE /order/delete?id=xxx`（删除时需考虑是否恢复库存）。
- `PUT /order/updateStatus`（仅更新状态）。

**数据库表建议**：`order_main`（id、order_no、member_id、member_name、pet_id、pet_name、total_amount、status、order_time、create_time、update_time）；`order_item`（id、order_id、goods_id、goods_name、quantity、price、amount）。

---

### 5.11 索赔记录模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/claim`；前端：如 `/claim/list` |
| 功能 | 索赔申请的**查询、新增、编辑、删除**、详情。 |

**列表与查询**

- 搜索：索赔编号、宠物名称、索赔原因（关键词）；时间范围：开始日期 ~ 结束日期；搜索、重置。
- 列表字段：序号、索赔编号、宠物名称、事件日期、索赔金额、最终索赔金额、赔付方式、赔付时间；操作：详情、编辑、删除。
- 分页：默认 10 条/页。

**新增/编辑索赔**

- 表单：索赔编号（可自动生成）、宠物（关联宠物档案）、事件日期、索赔金额、最终索赔金额、赔付方式（下拉/字典）、赔付时间、原因、备注等。

**后端接口**

- `GET /claim/pageList`：Request 含 `current`、`size`、`claimNo`、`petName`、`reason`、`startDate`、`endDate`。
- `POST /claim/add`、`PUT /claim/update`、`DELETE /claim/delete?id=xxx`、`GET /claim/detail?id=xxx`。

**数据库表建议**：`claim_record`（id、claim_no、pet_id、pet_name、event_date、claim_amount、final_amount、pay_method、pay_time、reason、remark、create_time、update_time）。

---

### 5.12 管理员管理模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/admin`；前端：如 `/admin/list` |
| 功能 | 管理员账号的查询、新增、编辑、删除、密码重置 |

**列表与查询**

- 搜索：管理员账号、姓名、角色；搜索、重置。
- 列表字段：序号、账号、姓名、角色、创建时间、状态；操作：编辑、删除、密码重置。
- 分页：默认 10 条/页。

**新增/编辑表单**

- 账号（新增必填，编辑可禁用）、姓名、角色（如超级管理员/普通管理员）、状态（启用/禁用）。密码在新增时必填，编辑时可选（不填则不改密码）。

**密码重置**

- 操作列“密码重置”：弹窗输入新密码（可二次确认），提交后调用 `PUT /admin/resetPwd` 或 `PUT /admin/update` 仅更新密码。

**后端接口**

- `GET /admin/pageList`：Request 含 `current`、`size`、`username`、`name`、`role`。
- `POST /admin/add`、`PUT /admin/update`、`DELETE /admin/delete?id=xxx`、`GET /admin/detail?id=xxx`、`PUT /admin/resetPwd`（id + newPassword）。

**数据库表建议**：`sys_admin`（id、username、password、name、role、status、create_time、update_time）。注意：登录模块与此表共用。

---

### 5.13 采购管理模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/purchase`；前端：如 `/purchase/list` |
| 功能 | 记录采购信息（如猫粮、狗粮、遛狗绳等），支持查询、新增、编辑、删除、详情。 |

**列表与查询**

- 搜索：采购单号、商品名称、供应商（关键词）；时间范围：采购日期；搜索、重置。
- 列表字段：序号、采购单号、商品名称、规格/单位、采购数量、单价、金额、供应商、采购日期、备注；操作：详情、编辑、删除。
- 分页：默认 10 条/页。

**新增/编辑采购**

- 采购单号（可自动生成）、选择商品（从商品管理中选择，选填，用于入库增加库存）、商品名称（可手填或由选择商品带出）、规格、采购数量、单价、金额、供应商、采购日期（空则默认当前日期）、备注等。**新增采购保存后即视为入库**：增加对应商品库存并写库存流水。

**后端接口**

- `GET /purchase/pageList`：Request 含 `current`、`size`、`purchaseNo`、`goodsName`、`supplier`、`startDate`、`endDate`。
- `POST /purchase/add`、`PUT /purchase/update`、`DELETE /purchase/delete?id=xxx`、`GET /purchase/detail?id=xxx`。

**数据库表建议**：`purchase_record`（id、purchase_no、goods_id、goods_name、spec、quantity、price、amount、supplier、purchase_date、remark、create_time、update_time）。

---

### 5.14 库存管理与库存流水模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/inventory`、`/inventoryFlow`；前端：如 `/inventory/list`、`/inventoryFlow/list` |
| 功能 | **库存管理**：查看各商品当前库存，支持按商品筛选。**库存流水**：记录每次入库、出库，支持按商品、流水类型、时间范围筛选。**数量规则**：库存由**采购管理入库**增加、**订单管理出库**（订单状态改为已发货/已完成）减少。 |

**库存管理**

- 列表：商品名称、商品编码、当前库存、单位、最近入库/出库时间（可选）；数据来源于商品表当前库存（由采购入库与订单出库维护）；支持搜索、分页。

**库存流水**

- 列表字段：序号、流水号、商品名称、类型（入库/出库）、数量、变更后库存、关联单号（采购单号/订单号等）、业务时间、备注；操作：详情。
- 搜索：商品名称、类型、开始时间～结束时间；分页。

**后端接口**

- 库存：`GET /inventory/pageList`（或 list），返回商品及当前库存。
- 流水：`GET /inventoryFlow/pageList`，Request 含 `current`、`size`、`goodsName`、`flowType`、`startTime`、`endTime`。

**数据库表建议**：沿用 `goods` 表库存字段（stock），单独流水表 `inventory_flow`（id、flow_no、goods_id、goods_name、flow_type、quantity、after_quantity、ref_no、flow_time、remark、create_time）。**采购新增**时入库增加 `goods.stock` 并写流水；**订单状态改为已发货/已完成**时出库扣减 `goods.stock` 并写流水。

---

### 5.15 数据字典管理模块

| 项目 | 说明 |
|------|------|
| 模块路径 | 后端：`/dict`；前端：如 `/dict/list` |
| 功能 | 维护系统字典（类型 + 字典项），供各页面下拉/选择使用，减少手工输入。 |

**字典类型**

- 如：会员等级、赔付方式、订单状态、宠物品种、商品分类等。类型编码（dict_type）、类型名称。

**字典项**

- 归属某类型，包含：标签（label）、值（value）、排序、状态。列表按类型筛选，支持新增、编辑、删除字典项。

**约定**

- 所有涉及固定选项的输入项，优先从数据字典取值；前端下拉从接口 `GET /dict/items?dictType=xxx` 获取选项。

**后端接口**

- `GET /dict/typeList` 或分页查询类型；`GET /dict/pageList` 字典项分页（按类型筛选）；`GET /dict/items?dictType=xxx` 返回某类型下所有可用项（供下拉）。
- `POST /dict/add`、`PUT /dict/update`、`DELETE /dict/delete?id=xxx`（字典项）；类型与项的增删改按需扩展。

**数据库表建议**：`sys_dict_type`（id、dict_type、dict_name、remark、sort）；`sys_dict_item`（id、dict_type、item_label、item_value、sort、status、create_time、update_time）。

---

## 六、通用页面与接口约定

### 6.1 分页

- 所有列表接口统一分页：请求参数 `current`（当前页）、`size`（每页条数，默认 10）；返回含 `records`、`total`、`current`、`size` 等，与 MyBatis-Plus 的 `IPage` 或自定义分页对象一致。

### 6.2 查询与重置

- 每个列表页提供“搜索”“重置”按钮；重置清空所有筛选条件并重新请求第一页。

### 6.3 新增/编辑

- 使用弹窗 + 表单；必填项、手机号、金额、日期等做前端校验；提交前校验，失败提示不提交。

### 6.4 删除

- 删除前二次确认（如 Element-UI MessageBox）；确认后调用删除接口，成功后刷新列表并提示。

### 6.5 风格与交互

- 统一使用 Element-UI；列表、表单、弹窗风格一致；接口请求时显示 loading，失败统一错误提示，成功根据业务给出成功提示。

### 6.6 日期默认值

- **所有页面**在新增、编辑时，若**日期/日期时间字段为空**，后端或前端应**默认为当前日期**（或当前日期时间），避免提交空日期导致校验或展示异常。

### 6.7 移动端适配

- 断点：窗口宽度 **&lt; 768px** 视为移动端（侧栏收起、顶部菜单按钮展开抽屉）。
- 列表**操作列**：移动端下操作列收窄（约 56px），仅显示「操作」入口，**单击后**以下拉菜单形式展示「详情、编辑、删除」等，避免操作列过宽占屏。
- 通用组件：`TableActionCell` 根据是否移动端自动切换为下拉或横向按钮；列宽使用 `actionColumnWidth`（由 mixin 提供）。

### 6.8 中文编码

- **后端**：`application.yml` 中配置 `server.servlet.encoding.charset=UTF-8`、`force: true`；JSON 使用 UTF-8；数据库连接使用 `characterEncoding=UTF-8`（JDBC 使用 Java 编码名，勿用 utf8mb4）。
- **数据库**：库与表字符集建议 **utf8mb4**；执行含中文的 SQL 脚本时，客户端需指定 `--default-character-set=utf8mb4`，且避免通过 PowerShell 管道传参（易乱码），推荐 cmd 下 `mysql ... < 文件.sql`。

---

## 七、新需求规划（分期实现）

以下为扩展需求，可在基础功能稳定后**分期实现**。每项包含**业务流程设计**与**开发指导思路**，供 Cursor / 开发实现时参考。

---

### 7.1 会员生命周期管理（已实现）

**业务流程设计**

- **会员等级规则**：设定消费金额/频次与等级的对应关系（如：普通会员 0–1000 元，白银会员 1000–3000 元，黄金会员 3000–10000 元，钻石会员 10000 元以上）。
- **自动升降级**：每月 1 日自动计算所有会员近 12 个月消费总额，根据规则调整等级。
- **流失预警机制**：设置预警周期（如 90 天），系统每日扫描最后消费时间超过预警天数的会员。
- **沉睡激活**：筛选注册超过 30 天但从未消费的会员，标记为「沉睡」，自动触发激活流程。

**当前实现概要**

- **会员表扩展**：`last_consume_time`（最后消费时间）、`tags`（JSON 标签）；订单/充值完成时更新最后消费时间。
- **等级规则表** `member_level_rule`：等级名称、消费金额上下限、排序；后端 `GET /memberLevelRule/pageList`、`/listAll`、增删改查；前端「会员生命周期」→「等级规则」Tab 维护。
- **生命周期配置表** `member_lifecycle_config`：如 `churn_warning_days`（流失预警天数）、`sleeping_register_days`（沉睡注册天数）；前端「生命周期配置」Tab 编辑。
- **预警记录表** `member_lifecycle_warning`：流失预警（CHURN）、沉睡（SLEEPING）；`GET /memberLifecycleWarning/pageList`；前端「流失与沉睡预警」Tab 列表与筛选。
- **定时任务**（Spring Schedule）：每月 1 日 02:00 等级计算；每日 03:00 流失扫描、03:10 沉睡扫描；可手动触发 `POST /memberLifecycle/runLevelCalc`、`runChurnScan`、`runSleepingScan`。
- **前端路由**：`/memberLifecycle`，Tab：等级规则、流失与沉睡预警、生命周期配置。

---

### 7.2 宠物档案深度管理

**业务流程设计**

- **年龄自动计算**：根据出生日期自动计算宠物年龄（月龄/岁龄），年龄分段：幼宠（<1 岁）、成宠（1–7 岁）、老年（>7 岁）。
- **品种关怀规则**：建立品种与常见健康问题的对应关系（如法斗-皮肤病、柯基-腰椎问题）。
- **重要日期提醒**：生日前 7 天、到家纪念日前 3 天自动生成提醒任务。
- **家庭关联**：同一会员的多只宠物自动关联，可设置「家庭默认宠物」。

**开发指导思路**

- 宠物表扩展：增加出生日期、到家日期、品种 ID、毛色、特征等字段。
- 品种字典表：建立品种表，包含品种名称、常见疾病、护理建议等。
- 提醒任务表：设计提醒任务表（提醒类型、目标 ID、提醒时间、状态）。
- 关联查询：查询会员时，同时查询该会员的所有宠物信息。

---

### 7.3 会员储值卡/套餐卡

**业务流程设计**

- **储值卡**：客户充值 X 元得 Y 元（或赠送积分），余额可消费所有项目，支持多次充值。
- **次卡套餐**：如「洗澡 10 次卡」售价 800 元，每次消费扣除 1 次，有效期 1 年。
- **亲情卡**：主卡充值 1000 元，可绑定最多 3 张副卡，副卡消费从主卡扣除。
- **消费扣款规则**：优先扣除次卡次数，不足再扣除储值余额，再不足现金支付。

**开发指导思路**

- 卡类型表：定义卡类型（储值卡/次卡）、面值、售价、有效期、赠送规则。
- 会员卡表：记录会员持有的卡（卡类型 ID、余额/剩余次数、有效期、状态）。
- 消费扣款逻辑：复杂的扣款优先级算法，支持组合支付。
- 交易流水表：记录所有充值、消费、赠送、过期等流水。

---

### 7.4 预约全流程管理

**业务流程设计**

- **预约创建**：支持电话预约（店员代建）、微信预约（客户自助）、到店预约。
- **时段管理**：设置每日可预约时段（如 09:00–18:00，每 1 小时一个时段）。
- **预约冲突检测**：同一美容师/医生在同一时段只能有一个预约。
- **预约状态流转**：待确认→已确认→已到店→服务中→已完成，或→已取消→爽约。
- **爽约规则**：爽约 3 次自动加入黑名单，需联系客服解除。

**开发指导思路**

- 预约表：记录预约单号、客户 ID、宠物 ID、服务项目、预约时间、美容师 ID、状态。
- 排班表：员工每日可预约时段、最大预约量。
- 提醒发送：预约前 24 小时和 2 小时，查询待提醒预约，发送消息。
- 冲突检测算法：检查同一员工同一时段已有预约数量是否已达上限。

---

### 7.5 商品库存预警

**业务流程设计**

- **库存上下限**：为每个商品设置安全库存上限（如最多存 100 件）和下限（如最少留 10 件）。
- **预警触发**：商品库存低于下限时，生成「采购预警」；高于上限时，生成「库存积压预警」。
- **保质期管理**：商品入库时记录生产日期和保质期，到期前 30 天、15 天、7 天分级预警。
- **盘点流程**：新建盘点单→录入实盘数量→系统自动计算盈亏→审核确认→库存调整。

**开发指导思路**

- 商品表扩展：增加库存上限、库存下限、保质期天数字段。
- 入库记录表：记录每批商品的批次号、生产日期、有效期、入库数量、剩余数量。
- 定时任务：每日检查库存预警和保质期预警，生成预警记录。
- 盘点模块：设计盘点单、盘点项、盘点差异报表。

---

### 7.6 智能采购建议

**业务流程设计**

- **采购预测算法**：基于过去 3 个月平均销量×采购周期（如 7 天到货）×安全系数（如 1.2）− 当前库存 − 在途库存。
- **供应商比价**：同一商品可关联多个供应商，记录历史采购价，自动推荐最低价供应商。
- **采购流程**：生成采购建议单→人工调整→转为采购订单→发送供应商→收货入库。
- **成本核算**：支持移动平均法（(原库存金额+新采购金额)/(原库存数量+新采购数量)）计算成本。

**开发指导思路**

- 采购建议算法：编写复杂的 SQL 或 Java 计算逻辑，考虑季节性因素。
- 供应商表：供应商基本信息、联系人、账期、合作状态。
- 采购价历史表：记录每次采购的价格、供应商，用于比价分析。
- 采购订单状态：待审核、已确认、部分收货、已完成、已取消。

---

### 7.7 商品组合销售

**业务流程设计**

- **套餐组合**：将多个商品/服务打包为一个套餐，设置套餐价（通常低于单品总和）。
- **关联推荐规则**：基于历史销售数据，统计「买了 A 的人也买了 B」的关联规则。
- **促销活动**：满减（满 200 减 30）、满赠（满 2 件送赠品）、折扣（第二件 8 折）。
- **促销叠加规则**：设置是否可叠加使用、叠加顺序（先折扣后满减）。

**开发指导思路**

- 套餐表：套餐名称、套餐价、有效期、包含的商品/服务列表（JSON 格式）。
- 促销活动表：活动类型、规则参数（如满 200 减 30）、时间范围、适用商品范围。
- 购物车计算：复杂的促销计算引擎，支持多重规则嵌套。
- 推荐算法：初期可手动配置关联，后期可基于订单数据计算关联度。

---

### 7.8 疫苗/驱虫提醒

**业务流程设计**

- **免疫计划模板**：建立常见宠物免疫计划（如犬：首年 3 针卫佳伍，以后每年 1 针）。
- **自动计算下次时间**：根据上次接种日期和疫苗类型，自动计算下次应接种日期。
- **群体提醒**：按疫苗类型筛选所有需要接种的宠物，批量生成提醒任务。
- **驱虫日历**：内外驱不同周期（内驱 3 个月/次，外驱 1 个月/次），自动计算下次时间。

**开发指导思路**

- 疫苗字典表：疫苗名称、适用动物、剂次间隔天数、有效期（年）。
- 免疫记录表：宠物 ID、疫苗 ID、接种日期、下次接种日期、疫苗批号、医生。
- 驱虫记录表：类似疫苗表，增加驱虫类型（内驱/外驱）。
- 批量提醒算法：查询所有下次接种日期≤今天的记录，生成提醒任务。

---

### 7.9 多维度财务报表

**业务流程设计**

- **日报**：每日收入（按现金/微信/支付宝分类）、支出、毛利、交易笔数。
- **月报**：月度收入构成（服务/商品/充值）、成本分析、毛利率、同比/环比。
- **年/季报**：累计数据、趋势分析、TOP10 分析。
- **自定义报表**：支持选择时间范围、对比周期、分类维度。

**开发指导思路**

- 财务流水表：记录每笔交易的金额、类型（收入/支出）、科目（服务/商品/充值）、支付方式。
- 成本记录：商品销售时自动记录成本（按成本核算方法）。
- 报表生成：使用 GROUP BY 和日期函数进行统计分析，可考虑使用数据库视图。
- 数据缓存：日报可实时计算，月报/季报可提前计算并缓存结果。

---

### 7.10 经营数据驾驶舱

**业务流程设计**

- **核心指标卡**：今日营收（实时/目标）、本月累计营收、会员总数、订单总数。
- **趋势图表**：近 7 天/30 天营收趋势、客流趋势。
- **排行榜**：热销商品 TOP10、高价值会员 TOP10、员工业绩 TOP5。
- **客流热力图**：按小时统计客流分布，高峰期标注。
- **客户画像**：新老客占比、会员消费占比、客单价分布。

**开发指导思路**

- 数据聚合：多个统计查询组合，注意性能优化。
- ECharts 集成：准备前端图表所需的数据格式（如柱状图、折线图、饼图）。
- 定时刷新：核心指标可实时，趋势数据可每小时/每天刷新。
- 数据权限：不同角色看到不同数据范围（店长看全店，店员只看自己）。

---

### 7.11 应收账款管理

**业务流程设计**

- **挂账开通**：仅限优质会员开通挂账权限，设置挂账额度（如 2000 元）和账期（如 30 天）。
- **挂账消费**：消费时选择挂账支付，生成应收账款。
- **催款流程**：到期前 7 天发送提醒，到期当天再次提醒，逾期 7 天发送警告。
- **坏账处理**：确认无法收回的欠款，经审批后核销，记录坏账原因。

**开发指导思路**

- 会员表扩展：增加挂账额度、已用额度、挂账状态字段。
- 应收账款表：关联订单号、金额、到期日、状态、催款记录。
- 催款记录表：记录每次催款时间、方式、结果。
- 定时任务：每日检查到期账款，更新状态，生成催款任务。

---

### 7.12 优惠券/卡券管理

**业务流程设计**

- **券类型**：满减券（满 100 减 20）、折扣券（8 折）、现金券（直接抵扣 10 元）、体验券（免费洗澡）。
- **发券方式**：手动发券（店员给特定客户）、自动发券（新会员注册、会员生日、消费达标）。
- **领券中心**：客户可自行领取公开券，设置每人限领数量。
- **核销统计**：统计发券数量、核销数量、核销率、带动消费金额。

**开发指导思路**

- 优惠券模板表：券名称、类型、面值/折扣、满减条件、有效期、发放总量、每人限领。
- 会员券表：会员 ID、券模板 ID、领取时间、使用时间、状态。
- 核销逻辑：计算订单金额是否满足使用条件，支持多券叠加规则。
- 统计分析：按券模板统计发放量、核销量、核销订单总额。

---

### 7.13 积分商城

**业务流程设计**

- **积分获取规则**：消费 1 元得 1 积分、签到得 5 积分、分享得 10 积分、评价得 20 积分。
- **积分兑换**：设置积分商品（如狗粮试用装、玩具），消耗积分兑换。
- **积分抵扣**：消费时选择用积分抵扣现金（如 100 积分=1 元），设置抵扣上限。
- **积分有效期**：设置积分有效期（如 1 年），到期前 30 天提醒，到期自动清零。

**开发指导思路**

- 积分规则表：规则名称、获取方式、奖励积分、每日上限。
- 积分流水表：会员 ID、变动类型（获取/消费/过期）、变动数量、余额、关联业务 ID。
- 积分商品表：商品名称、所需积分、库存、兑换限制。
- 定时任务：每月/每日检查过期积分，生成过期流水。

---

### 7.14 会员评价与反馈

**业务流程设计**

- **评价触发**：服务完成后 24 小时内，自动发送评价邀请。
- **评价维度**：服务态度、专业技能、环境卫生、性价比（1–5 星）。
- **差评处理**：3 星及以下自动标记为差评，分配给店长处理。
- **投诉流程**：客户投诉→受理→调查→处理→回访→结案。

**开发指导思路**

- 评价表：关联订单 ID、各维度评分、评语、匿名标识。
- 投诉表：投诉人、投诉对象、投诉内容、处理人、处理结果、回访记录。
- 统计报表：平均分趋势、差评原因分析、员工好评率排行。
- 消息通知：差评/新投诉时，实时通知相关责任人。

---

### 7.15 客户关怀任务

**业务流程设计**

- **售后回访**：服务后 3 天，自动创建回访任务，询问宠物恢复情况。
- **节日关怀**：春节、中秋、国庆等节日前 3 天，自动发送节日祝福。
- **天气关怀**：高温预警（>35℃）、寒潮预警（<0℃）时，发送宠物护理提醒。
- **任务分配**：关怀任务自动分配给会员专属顾问或随机分配。

**开发指导思路**

- 关怀模板库：预设多种关怀场景的短信/微信模板。
- 任务生成器：根据规则（时间/天气）自动生成关怀任务。
- 天气 API 集成：对接第三方天气 API，获取本地天气预警。
- 任务跟踪：记录任务执行人、执行时间、客户反馈。

---

### 7.16 寄养管理

**业务流程设计**

- **寄养预约**：选择入住/离店时间、笼位类型（标准/豪华）、特殊要求。
- **笼位管理**：可视化笼位图，不同颜色标识空闲/占用/预订/清洁。
- **入住流程**：登记宠物信息、签署寄养协议、预收押金、分配笼位。
- **寄养日报**：每日拍照/视频，记录饮食、排便、活动情况，推送给主人。
- **离店结算**：自动计算住宿费、额外消费（加餐、洗澡）、退还押金。

**开发指导思路**

- 笼位表：笼位编号、类型、位置、最大容量（如可放多只同窝宠物）。
- 寄养订单表：关联客户、宠物、笼位、入住/离店时间、押金、状态。
- 寄养日报表：每日记录体温、饮食量、排便情况、照片 URL。
- 费用计算器：按小时/按天计费规则、节假日加价规则。

---

### 7.17 宠物接送服务

**业务流程设计**

- **接送预约**：客户选择接送地址、时间、宠物、服务类型（洗澡/看病/寄养）。
- **司机派单**：根据司机位置、工作量智能派单，支持手动改派。
- **状态跟踪**：待接单→已接单→已出发→已接到→送达中→已送达。
- **费用计算**：基础费+距离费+等待费，支持夜间/节假日加价。
- **电子签收**：送达后客户电子签名确认。

**开发指导思路**

- 接送订单表：关联原始服务订单、司机 ID、接送地址、时间、费用。
- 司机表：司机信息、车辆信息、服务范围、在线状态。
- 地图 API 集成：对接高德/百度地图，实现距离计算、路径规划。
- 派单算法：简单规则（就近派单），后期可升级为智能派单。

---

### 7.18 活体销售管理

**业务流程设计**

- **幼宠信息录入**：品种、出生日期、父母信息、疫苗情况、售价、照片/视频。
- **成长记录**：每周更新照片/视频，记录体重变化、疫苗接种进度。
- **预订管理**：客户支付预订金锁定幼宠，记录预订金金额、尾款金额。
- **售后保障**：售出后 7 天健康保障期，记录保障期内健康咨询和问题处理。

**开发指导思路**

- 幼宠表：关联品种 ID、出生日期、售价、状态（待售/已预订/已售）。
- 成长记录表：记录日期、体重、照片 URL、视频 URL、备注。
- 预订表：关联客户 ID、幼宠 ID、预订金、尾款、预订时间、提货时间。
- 健康保障记录：保障期内咨询/就诊记录。

---

### 7.19 宠物证件管理

**业务流程设计**

- **证件上传**：支持拍照上传血统证书、疫苗本、检疫证明等证件。
- **证件分类**：按证件类型分类管理，支持多证件关联同一宠物。
- **到期提醒**：设置证件有效期（如检疫证明有效期 7 天），到期前提醒。
- **电子证照**：生成带二维码的电子证件，可分享或打印。

**开发指导思路**

- 证件表：宠物 ID、证件类型、证件编号、发证机关、有效期、文件 URL。
- 文件存储：支持本地存储或 OSS 对象存储（阿里云/七牛云）。
- 二维码生成：根据宠物 ID 和证件 ID 生成二维码，扫码可查看电子证件。
- 到期提醒：每日检查即将到期的证件，生成提醒任务。

---

## 八、项目运行步骤

1. **数据库**：MySQL 创建数据库，执行项目提供的 SQL 脚本（建表 + 初始数据，如默认 admin 账号）。
2. **后端**：修改 `application.properties`/`application.yml` 中数据源（url、username、password），运行 `JsonBaseApplication.java`，默认端口 8080。
3. **前端**：根目录执行 `npm install`，再 `npm run serve`；浏览器访问配置的登录地址（如 localhost:8080 或前端端口），使用 admin/admin 登录。

---

## 九、文档使用说明（给 Cursor / 开发者）

- 开发新模块时：按“模块路径 → 表设计 → Entity → Mapper → Service → Controller → Request”顺序实现；前端按“路由 → API 封装 → 列表页 + 表单弹窗”实现。
- 命名保持一致：实体类与表名对应；Controller 路径与前端 API 路径一致；Request 类与列表筛选条件一致。
- 本文档未明确规定的字段（如排序、状态码值），可按业务自行扩展，并在同一模块内保持统一。

以上为宠物店管理系统的完整功能说明，可直接作为 Cursor 生成与核对代码的依据。
